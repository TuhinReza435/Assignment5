Var : Var is a function scope ,if we declar any place in a function then it work whole function, it's reassignable and redeclarable

Let : it's a block scope variable it work in a block . reassignable possible but redeclarable impossible

const : it'a a block scope . reassign and redeclar not possible 


(...) Spread Operator : Allow an iterable to be expanded into indivudual elements or properties. it use to make object and array induvidual element

forEach ()=> just a simple loop doesnot return anything 
Map()=> Create a new arry by applying a function of the original array
Filter => Create a new arry only target value which pass the conditions


arrow function : Arrow function is a function which allow use to skip function keyword

Template Literal : When we use any string use ("" or ' ') But when we need both and variable into that portion we use template literal (``)